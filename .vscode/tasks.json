{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "deploy-arduino",
            "type": "shell",
            "command": "arduino --upload growpi.ino --port /dev/ttyACM0",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}